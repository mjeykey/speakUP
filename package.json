{
  "name": "speakup",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 4173",
    "check:syntax": "find src -name '*.js' -print0 | xargs -0 -n1 node --check",
    "qa": "node qa-check.mjs",
    "test": "npm run check:syntax && npm run qa"
  }
}
