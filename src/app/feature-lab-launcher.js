// Archived feature lab launcher.
// The Siren Project and What's Around You test modules remain in the repository,
// but are intentionally not mounted in the live SpeakUP interface.
export function mountArchivedFeatureLab() {
  return false;
}
