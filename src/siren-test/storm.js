let rafId=null;
export function stormMarkup(){return `<canvas class="storm" id="stormCanvas"></canvas><div class="flash" id="flash"></div>`}
export function startStormCanvas(){cancelAnimationFrame(rafId);const c=document.getElementById('stormCanvas');if(!c)return;const x=c.getContext('2d');let W,H,drops=[];function resize(){W=c.width=innerWidth*devicePixelRatio;H=c.height=innerHeight*devicePixelRatio;c.style.width=innerWidth+'px';c.style.height=innerHeight+'px';drops=Array.from({length:Math.min(520,Math.floor(innerWidth*.7))},()=>({x:Math.random()*W,y:Math.random()*H,r:(1.1+Math.random()*2.2)*devicePixelRatio,s:(6+Math.random()*11)*devicePixelRatio,a:.2+Math.random()*.45}))}resize();window.onresize=resize;let t=0;function draw(){t+=.012;x.clearRect(0,0,W,H);const g=x.createLinearGradient(0,0,0,H);g.addColorStop(0,'#071521');g.addColorStop(.58,'#07111a');g.addColorStop(1,'#010307');x.fillStyle=g;x.fillRect(0,0,W,H);const horizon=H*.62;x.fillStyle='#020a11';x.fillRect(0,horizon,W,H-horizon);for(let layer=0;layer<4;layer++){x.beginPath();const base=horizon+layer*H*.075;for(let px=0;px<=W;px+=12*devicePixelRatio){const yy=base+Math.sin(px/(95*devicePixelRatio)+t*(1.3+layer*.35)+layer)*((15+layer*9)*devicePixelRatio)+Math.sin(px/(37*devicePixelRatio)-t*2)*(5*devicePixelRatio);px?x.lineTo(px,yy):x.moveTo(px,yy)}x.lineTo(W,H);x.lineTo(0,H);x.closePath();x.fillStyle=`rgba(${8+layer*4},${30+layer*8},${43+layer*10},${.9-layer*.08})`;x.fill()}for(const d of drops){x.beginPath();x.arc(d.x,d.y,d.r,0,Math.PI*2);x.fillStyle=`rgba(210,233,242,${d.a})`;x.fill();d.x-=d.s*.04;d.y+=d.s;if(d.y>H){d.x=Math.random()*W;d.y=-10*devicePixelRatio}}rafId=requestAnimationFrame(draw)}draw()}
export function stopStormCanvas(){cancelAnimationFrame(rafId)}